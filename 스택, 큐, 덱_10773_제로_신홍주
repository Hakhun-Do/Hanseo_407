import sys

k = int(input())

stack = []

for i in range(k):
    temp = int(sys.stdin.readline().rstrip())

    if temp == 0:
        stack.pop()
    else:
        stack.append(temp)

print(sum(stack))
