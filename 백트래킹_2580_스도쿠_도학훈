//백트래킹_2580_스도쿠_도학훈
#include<iostream>
using namespace std;
int sudoku[9][9]; //스도쿠 나타내줌
int check[10]; //들어갈 수 있는 숫자 나타내줌
//들어갈 수 있는 숫자 확인
void row_col_sq(int m,int n) { 
	// check[] 초기화
	for (int i = 0; i < 9; i++) {
		check[i] = 0;
	}
	// row,col 확인
	for (int i = 0; i < 9; i++) {
		check[sudoku[m][i]] = 1;
		check[sudoku[i][n]] = 1;
	}
	// square 확인
	if (m < 3 && n < 3) {
		for (int i = 0; i < 3; i++) {
			for (int j = 0; j < 3; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
	else if (m < 3 && n >= 3 && n < 6) {
		for (int i = 0; i < 3; i++) {
			for (int j = 3; j < 6; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
	else if (m < 3 && n >= 6) {
		for (int i = 0; i < 3; i++) {
			for (int j = 6; j < 9; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
	else if (m >= 3 && m < 6 && n < 3) {
		for (int i = 0; i < 3; i++) {
			for (int j = 0; j < 3; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
	else if (m >= 3 && m < 6 && n >= 3 && n < 6) {
		for (int i = 0; i < 3; i++) {
			for (int j = 3; j < 6; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
	else if (m >= 3 && m < 6 && n >= 6) {
		for (int i = 0; i < 3; i++) {
			for (int j = 6; j < 9; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
	else if (m >= 6 && n < 3) {
		for (int i = 0; i < 3; i++) {
			for (int j = 0; j < 3; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
	else if (m >= 6 && n >= 3 && n < 6) {
		for (int i = 0; i < 3; i++) {
			for (int j = 3; j < 6; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
	else if (m >= 6 && n >= 6) {
		for (int i = 0; i < 3; i++) {
			for (int j = 6; j < 9; j++) {
				check[sudoku[i][j]] = 1;
			}
		}
	}
}
void go(int m,int n) {
	if (m == 8 && n==9) {
		return;
	}
	for (int i = m; i < 9; i++) {
		for (int j = n; j < 9; j++) {
			if (sudoku[i][j] == 0) {
				row_col_sq(i, j);
				for (int p = 0; p < 9; p++) {
					if (check[p] == 0) {
						sudoku[i][j] = p;
						go(i, j + 1);
					}
					else return;
				}
			}
		}
	}
	
}
int main() {
	//스도쿠 입력
	for (int i = 0; i < 9; i++) {
		for (int j = 0; j < 9; j++) {
			cin >> sudoku[i][j];
		}
	}
	go(0, 0); // go 함수로 이동
	//스도쿠 출력

	for (int i = 0; i < 9; i++) {
		for (int j = 0; j < 9; j++) {
			cout << sudoku[i][j]<<" ";
		}
		cout << "\n";
	}
}
