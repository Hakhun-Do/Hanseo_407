#include <iostream>
using namespace std;
int main() {
	int n, k, p = 0;
	cin >> n >> k;
	for (int i = 1; i <= n; i++) {
		if (n % i == 0)
			k--;
		if (k==0){
			p = i;
			break;
		}
	}
	cout << p;
}
