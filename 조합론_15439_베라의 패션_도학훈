//조합론_15439_베라의 패션_도학훈
#include<iostream>
#include<vector>
using namespace std;

int main() {

    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;
    cin >> n;
    if (n == 1)cout << 0;
    else cout << n * (n - 1);

    return 0;
}
 
