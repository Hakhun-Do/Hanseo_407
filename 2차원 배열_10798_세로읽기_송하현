#include <iostream>
#include <string>
using namespace std;
int main() {
	int m = 0;
	char arr[5][16]={'\0', };
	for (int i = 0; i < 5; i++)
		cin >> arr[i];
	for (int i = 0; i < 16; i++)
	{
		for (int j = 0; j < 5; j++)
		{
			if(arr[j][i]!='\0')
				cout << arr[j][i];
		}
	}
}
