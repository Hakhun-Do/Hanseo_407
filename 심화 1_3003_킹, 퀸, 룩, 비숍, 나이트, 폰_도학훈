//심화 1_3003_킹, 퀸, 룩, 비숍, 나이트, 폰_도학훈
#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main() {

	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	
	int chess[6] = { 1,1,2,2,2,8 };
	int temp;
	for (int i = 0; i < 6; i++) {
		cin >> temp;
		chess[i] = chess[i] - temp;
	}
	for (int i = 0; i < 6; i++) {
		cout << chess[i] << " ";
	}
	return 0;
}
